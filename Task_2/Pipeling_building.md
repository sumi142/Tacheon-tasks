Task 2 :

Need to build a pipeline that will pull the data from public API , clean the data, transform the data and load the data into BQ.

API : 
Weather API - because it is a free API

Getting Data :
Fetching the data from API using GET requests and response will come in json format.
Converting the data into a tabular format(Dataframe here).

Transforming the data :
I have transformed the data with a derived columns.

Loading the data :
I have created a dataset and table in BQ with datatypes and column names as per the data we have and I have authenticated using a service account with a private key(Json key).



